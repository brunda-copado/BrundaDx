<apex:component access="global">
    <div style="margin: 30px auto; color: #272525; font-size: 9px;">
        <div style="padding-bottom: 15px;">
            <apex:outputText value="{!$Label.copado__emails_footer_title}">
                <apex:param value="{!YEAR(TODAY())}" />
            </apex:outputText>
        </div>
        <div>
            {!$Label.copado__emails_footer_text}
        </div>
    </div>
</apex:component>