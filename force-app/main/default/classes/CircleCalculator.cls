public class CircleCalculator {

    // Method to calculate the area of a circles
    //Test RB
    public static Decimal calculateCircleArea(Decimal radius) {
        return Math.PI * radius * radius;
    }
}